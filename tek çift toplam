print("Aralıkta bulunan sayıların tek ve çift değerlerin ayrı ayrı toplamını bulan programa hoşgeldiniz.")
sayi=int(input("Bir sayı giriniz :"))
tek_toplam=0
cift_toplam=0
for i in range (1,sayi+1):
    if i%2==0:
        cift_toplam+=i
    else:
        tek_toplam+=i
print("Tek sayıların toplamı : "+str(tek_toplam)+" \nÇift sayıların toplamı : "+str(cift_toplam))
